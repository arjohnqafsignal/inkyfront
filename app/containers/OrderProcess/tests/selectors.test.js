// import { selectOrderProcessDomain } from '../selectors';

describe('selectOrderProcessDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
